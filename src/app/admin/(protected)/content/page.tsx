import Link from "next/link";
import { Plus } from "lucide-react";
import { createClient } from "@/lib/supabase/server";

export default async function ContentListPage() {
  const client = await createClient(); const { data } = client ? await client.from("content_entries").select("id, kind, slug, title, status, updated_at").order("updated_at", { ascending: false }) : { data: [] };
  return <><div className="flex flex-wrap items-end justify-between gap-5"><div><p className="text-xs font-extrabold tracking-widest text-[#1974E2] uppercase">Structured publishing</p><h1 className="mt-2 text-4xl font-extrabold text-[#071127]">Content</h1></div><Link href="/admin/content/new" className="inline-flex min-h-11 items-center gap-2 rounded-lg bg-[#1974E2] px-4 font-bold text-white"><Plus size={17} />New entry</Link></div><div className="mt-8 overflow-x-auto rounded-2xl border border-[#E4EAF0] bg-white"><table className="w-full min-w-[680px] text-left text-sm"><thead className="bg-[#F4F7FA] text-xs text-[#667586] uppercase"><tr><th className="p-4">Title</th><th className="p-4">Type</th><th className="p-4">Status</th><th className="p-4">Updated</th></tr></thead><tbody>{(data || []).map((entry) => <tr key={entry.id} className="border-t border-[#E4EAF0]"><td className="p-4"><Link className="font-bold text-[#071127] hover:text-[#1974E2]" href={`/admin/content/${entry.id}`}>{entry.title}</Link><span className="block text-xs text-[#667586]">/{entry.slug}</span></td><td className="p-4">{entry.kind}</td><td className="p-4"><span className={entry.status === "published" ? "rounded-full bg-green-100 px-2 py-1 text-xs font-bold text-green-800" : "rounded-full bg-[#E4EAF0] px-2 py-1 text-xs font-bold text-[#586575]"}>{entry.status}</span></td><td className="p-4 text-[#667586]">{new Date(entry.updated_at).toLocaleDateString("en-GB")}</td></tr>)}</tbody></table>{!data?.length && <p className="p-8 text-center text-[#667586]">No CMS entries yet. The approved static launch content remains active.</p>}</div></>;
}
