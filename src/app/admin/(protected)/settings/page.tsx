import { saveSettings } from "../actions";
import { AdminField } from "@/components/admin/content-editor";
import { siteConfig } from "@/config/site";
import { createClient } from "@/lib/supabase/server";

export default async function SettingsPage() { const client = await createClient(); const { data } = client ? await client.from("site_settings").select("value").eq("id", true).maybeSingle() : { data: null }; const override = (data?.value || {}) as Partial<typeof siteConfig>; const current = { ...siteConfig, ...override, address: { ...siteConfig.address, ...(override.address || {}) }, openingHours: { ...siteConfig.openingHours, ...(override.openingHours || {}) } }; return <><p className="text-xs font-extrabold tracking-widest text-[#1974E2] uppercase">Single source of truth</p><h1 className="mt-2 text-4xl font-extrabold text-[#071127]">Business settings</h1><form action={saveSettings} className="mt-8 grid gap-5 rounded-2xl border border-[#E4EAF0] bg-white p-6 md:grid-cols-2"><AdminField label="Business name"><input name="name" required defaultValue={current.name} /></AdminField><AdminField label="Legal name"><input name="legalName" required defaultValue={current.legalName} /></AdminField><AdminField label="Company number"><input name="companyNumber" required defaultValue={current.companyNumber} /></AdminField><AdminField label="Phone"><input name="phone" required defaultValue={current.phone} /></AdminField><AdminField label="WhatsApp"><input name="whatsapp" required defaultValue={current.whatsapp} /></AdminField><AdminField label="Email"><input name="email" type="email" required defaultValue={current.email} /></AdminField><AdminField label="Maps URL"><input name="googleMapsUrl" type="url" defaultValue={"googleMapsUrl" in current ? String(current.googleMapsUrl || "") : ""} /></AdminField><AdminField label="Building"><input name="building" required defaultValue={current.address.building} /></AdminField><AdminField label="Street"><input name="street" required defaultValue={current.address.street} /></AdminField><AdminField label="Town"><input name="town" required defaultValue={current.address.town} /></AdminField><AdminField label="City"><input name="city" required defaultValue={current.address.city} /></AdminField><AdminField label="Postcode"><input name="postcode" required defaultValue={current.address.postcode} /></AdminField><h2 className="mt-3 text-2xl font-bold text-[#071127] md:col-span-2">Opening hours</h2>{Object.entries(current.openingHours).map(([day, hours]) => <AdminField key={day} label={day === "bankHolidays" ? "Bank holidays" : day.charAt(0).toUpperCase() + day.slice(1)}><input name={day} required defaultValue={hours} /></AdminField>)}<div className="flex items-end md:col-span-2"><button className="min-h-12 w-full rounded-xl bg-[#1974E2] px-5 font-bold text-white">Save settings</button></div></form></>; }
