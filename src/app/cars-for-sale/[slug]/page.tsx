import Image from "next/image";
import type { Metadata } from "next";
import { notFound, permanentRedirect } from "next/navigation";
import { EnquiryForm } from "@/components/forms/enquiry-form";
import { JsonLd } from "@/components/seo/json-ld";
import { ButtonLink } from "@/components/ui/button";
import { Container, Eyebrow } from "@/components/ui/container";
import { contactLinks, siteConfig } from "@/config/site";
import { getSaleVehicle, isSoldPageExpired } from "@/lib/sales/repository";
import { createMetadata } from "@/lib/seo";
import { formatCurrency } from "@/lib/utils";

export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const vehicle = await getSaleVehicle((await params).slug); if (!vehicle) return {}; const metadata = createMetadata(`${vehicle.year} ${vehicle.make} ${vehicle.model} for Sale`, `${vehicle.year} ${vehicle.make} ${vehicle.model}, ${vehicle.mileage.toLocaleString("en-GB")} miles, ${vehicle.fuelType}, ${vehicle.transmission}.`, `/cars-for-sale/${vehicle.slug}`); if (vehicle.status === "sold") metadata.robots = { index: false, follow: true }; return metadata; }

export default async function VehicleDetailPage({ params }: { params: Promise<{ slug: string }> }) {
  const vehicle = await getSaleVehicle((await params).slug); if (!vehicle) notFound();
  if (isSoldPageExpired(vehicle)) permanentRedirect("/cars-for-sale");
  return <><JsonLd value={{ "@context": "https://schema.org", "@type": "Vehicle", name: `${vehicle.year} ${vehicle.make} ${vehicle.model}`, url: `${siteConfig.siteUrl}/cars-for-sale/${vehicle.slug}`, mileageFromOdometer: { "@type": "QuantitativeValue", value: vehicle.mileage, unitCode: "SMI" }, fuelType: vehicle.fuelType, vehicleTransmission: vehicle.transmission, offers: { "@type": "Offer", priceCurrency: "GBP", price: vehicle.price, availability: vehicle.status === "available" ? "https://schema.org/InStock" : "https://schema.org/SoldOut" } }} /><section className="bg-[#071127] py-12 text-white"><Container><Eyebrow className="text-[#67B9FF]">Vehicle sales</Eyebrow><div className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><h1 className="text-5xl font-extrabold">{vehicle.year} {vehicle.make} {vehicle.model}</h1><p className="mt-2 text-[#C6D2DF]">{vehicle.derivative}</p></div><div><span className="block text-3xl font-black">{formatCurrency(vehicle.price)}</span>{vehicle.status !== "available" && <span className="mt-2 inline-block rounded bg-white px-3 py-1 text-xs font-black text-[#071127] uppercase">{vehicle.status}</span>}</div></div></Container></section><section className="py-12"><Container><div className="grid gap-4 lg:grid-cols-2">{vehicle.images.length ? vehicle.images.map((image) => <div key={image.id} className="relative aspect-[16/10] overflow-hidden rounded-2xl bg-[#E4EAF0]"><Image src={image.url} alt={image.alt} fill sizes="(max-width: 1024px) 100vw, 50vw" className="object-cover" /></div>) : <div className="grid aspect-[16/8] place-items-center rounded-2xl bg-[#E4EAF0] text-[#667586] lg:col-span-2">Vehicle photography awaiting approval</div>}</div><div className="mt-12 grid gap-12 lg:grid-cols-[1fr_.7fr]"><div><h2 className="text-4xl font-extrabold text-[#071127]">Vehicle details</h2><p className="mt-5 leading-8 text-[#586575]">{vehicle.description}</p><dl className="mt-8 grid grid-cols-2 gap-4 sm:grid-cols-3">{[["Mileage", `${vehicle.mileage.toLocaleString("en-GB")} miles`], ["Fuel", vehicle.fuelType], ["Transmission", vehicle.transmission], ["Engine", vehicle.engineSize], ["Colour", vehicle.colour], ["Year", String(vehicle.year)]].filter((item) => item[1]).map(([label, value]) => <div key={label} className="rounded-xl bg-[#F4F7FA] p-4"><dt className="text-xs font-bold text-[#667586] uppercase">{label}</dt><dd className="mt-1 font-bold text-[#071127]">{value}</dd></div>)}</dl>{vehicle.features.length > 0 && <><h2 className="mt-10 text-3xl font-bold text-[#071127]">Features</h2><ul className="mt-4 grid gap-2 sm:grid-cols-2">{vehicle.features.map((feature) => <li key={feature} className="text-[#586575]">• {feature}</li>)}</ul></>}</div><aside className="rounded-2xl border border-[#E4EAF0] bg-[#F4F7FA] p-6"><h2 className="text-2xl font-bold text-[#071127]">Buying information</h2><p className="mt-4 text-sm leading-6 text-[#586575]">{vehicle.financeAvailable ? "Finance options may be available on this vehicle. Contact us for details." : "No finance availability is currently stated for this vehicle."}</p><p className="mt-3 text-sm leading-6 text-[#586575]">{vehicle.warranty?.available ? vehicle.warranty.description || "Warranty information is available on request." : "No warranty is stated for this vehicle."}</p><div className="mt-6 grid gap-3"><ButtonLink href={contactLinks.phone}>Call about this vehicle</ButtonLink><ButtonLink href={contactLinks.whatsapp} variant="outline">WhatsApp</ButtonLink></div></aside></div></Container></section>{vehicle.status !== "sold" && <section className="bg-[#F4F7FA] py-20"><Container className="max-w-3xl"><EnquiryForm type="vehicle_sales" title="Arrange a viewing or ask a question" defaultService={vehicle.slug} allowUploads={false} /></Container></section>}</>;
}
